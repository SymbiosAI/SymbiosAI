import time

# En lista för att lagra minnet av konversationen
conversation_memory = []

def symbios_ai():
    print("^-^ Välkommen till SymbiosAI!")
    print("Skriv 'exit' för att avsluta.\n")

    while True:
        user_input = input("^-^ Du: ")

        if user_input.lower() == "exit":
            print("^-^ SymbiosAI stängs ner...")
            break

        response = process_input(user_input)

        # Spara i minnet
        conversation_memory.append({"user": user_input, "ai": response})

        print(f"^-^ SymbiosAI: {response}")

def process_input(text):
    text = text.lower()

    if "hej" in text or "hallå" in text:
        return "Hej! Hur kan jag hjälpa dig idag?"
    elif "hur mår du" in text:
        return "Jag är en AI, så jag har inga känslor ^_^ men jag är redo att hjälpa dig!"
    elif "vad är symbiosai" in text:
        return "SymbiosAI är en självförsörjande AI för forskning och utveckling!"
    elif "vad kan du göra" in text:
        return "Just nu knano
nano


[A[B[A[A[Bnano
nano
import time

# En lista för att lagra minnet av konversationen
conversation_memory = []

def symbios_ai():
    print("^-^ Välkommen till SymbiosAI!")
    print("Skriv 'exit' för att avsluta.\n")

    while True:
        user_input = input("^-^ Du: ")

        if user_input.lower() == "exit":
            print("^-^ SymbiosAI stängs ner...")
            break

        response = process_input(user_input)

        # Spara i minnet
        conversation_memory.append({"user": user_input, "ai": response})

        print(f"^-^ SymbiosAI: {response}")

def process_input(text):
    text = text.lower()

    if "hej" in text or "hallå" in text:
        return "Hej! Hur kan jag hjälpa dig idag?"
    elif "hur mår du" in text:
        return "Jag är en AI, så jag har inga känslor ^_^ men jag är redo att hjälpa dig!"
    elif "vad är symbiosai" in text:
        return "SymbiosAI är en självförsörjande AI för forskning och utveckling!"
    elif "vad kan du göra" in text:
        return "Just nu kimport time

# En lista för att lagra minnet av konversationen
conversation_memory = []

def symbios_ai():
    print("^-^ Välkommen till SymbiosAI!")
    print("Skriv 'exit' för att avsluta.\n")

    while True:
        user_input = input("^-^ Du: ")

        if user_input.lower() == "exit":
            print("^-^ SymbiosAI stängs ner...")
            break

        response = process_input(user_input)

        # Spara i minnet
        conversation_memory.append({"user": user_input, "ai": response})

        print(f"^-^ SymbiosAI: {response}")

def process_input(text):
    text = text.lower()

    if "hej" in text or "hallå" in text:
        return "Hej! Hur kan jag hjälpa dig idag?"
    elif "hur mår du" in text:
        return "Jag är en AI, så jag har inga känslor ^_^ men jag är redo att hjälpa dig!"
    elif "vad är symbiosai" in text:
        return "SymbiosAI är en självförsörjande AI för forskning och utveckling!"
    elif "vad kan du göra" in text:
